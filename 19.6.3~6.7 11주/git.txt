commit --amend
branch -f
checkout
cherry-pick ~
reset
revert
rebase -i
merge
pull --rebase
push

git checkout -b 'local에서 쓸 브랜치이름' remote/branchName => 리모트 브랜치를 로컬브랜치로 받아온다
git reset --hard 'commit 이름' => 리셋 쎄게

git 